print("세 자리의 암스트롱 수 : ", end='')

for num in range(100, 1000):
    hundreds = num // 100
    tens = (num // 10) % 10
    units = num % 10

    if num == (hundreds**3 + tens**3 + units**3):
        print(num, end=' ')
